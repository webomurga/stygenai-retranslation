# Retranslation at the intersection of style, machines, and plagiarism

## Documentation Structure

This repository contains two complementary documentation layers:

- **Study & Reproducibility (this page)**  
  Experimental design, corpus organization, and replication setup used in the accompanying paper.

- **Framework Documentation**  
  General usage of the feature extraction and authorship attribution pipeline:  
  👉 `README.packages.md`

## Experimental Corpus Organization

This study uses two primary experimental corpus partitions to support comparative stylometric analysis of human translations and machine translation systems.

To ensure reproducibility of the experiments reported in the paper, the dataset is organized into two independent corpus roots:

```text
corpus_data.human/
corpus_data.nmt/
```

Each corpus root contains its own aligned `_txt` and `_morph` directories, keyword files, generated vectors, and analysis outputs.

---

## 1. `corpus_data.human/`

This dataset contains:

* original human literary translations,
* reference corpora,
* and outputs generated by NMT systems trained on human translator data.

Example structure:

```text
corpus_data.human/
├── ae_txt/
├── ae_morph/
├── bd_txt/
├── bd_morph/
├── kk_txt/
├── kk_morph/
├── machine_txt/
├── machine_morph/
├── ref_txt/
└── ref_morph/
```

This configuration is primarily used for:

* Human vs Human stylistic comparison,
* Human vs Human-trained NMT analysis,
* measuring whether an NMT system reproduces translator-specific stylistic traits.

---

## 2. `corpus_data.nmt/`

This dataset contains corpora associated with machine-generated translation systems.

Example structure:

```text
corpus_data.nmt/
├── human_txt/
├── human_morph/
├── machine_txt/
├── machine_morph/
├── llm_txt/
├── llm_morph/
├── ref_txt/
└── ref_morph/
```

These datasets are used for:

* Human vs Commercial-NMT analysis,
* Human vs LLM-NMT analysis,
* NMT vs NMT comparison,
* cross-model stylistic attribution experiments.

---

## Experimental Context

As part of the study, three separate OPUS-NMT systems were trained using:

* human literary translations,
* commercial NMT translations,
* LLM-generated translations.

The resulting systems were then compared against:

* the human translators whose texts were used during training,
* other NMT systems,
* and reference corpora.

This structure allows the same stylometric and authorship attribution pipeline to be reused across both traditional literary translation analysis and neural machine translation style evaluation experiments.

---

## Translation Scripts

The `translation_scripts/` directory contains the scripts used to generate translations from eight different systems. Each script follows the same context-window approach: for every target sentence, a surrounding block of text (up to 1 000 characters) is built and passed to the API so that the model can resolve pronouns, tone, and references correctly.

Scripts that call LLMs instruct the model to translate **only** the target sentence and use the context block solely for disambiguation. Scripts that call traditional MT APIs (Azure, AWS, DeepL, Google) embed the target sentence between sentinel tags (`[[[TGT_START]]]` / `[[[TGT_END]]]`) so the translated target can be extracted from the full translated block automatically.

All scripts expect a `sentences` list to be defined before running (a list of English source sentences). Each script saves its output to an `.xlsx` file with `original` and `translated_target` columns (Azure and AWS also save a `translated_full` column containing the entire translated context block).

---

## Fine-tuning

The `finetuning/` directory contains the script used to fine-tune a MarianMT model on the study corpus using LoRA (Low-Rank Adaptation).

**`finetune_marian_lora.py`**

Three separate fine-tuned models were produced this way: one trained on human literary translations, one on commercial NMT output, and one on LLM-generated translations. Their outputs were subsequently fed into the analysis pipeline.
